const STUDENT_ID_REGEX = /^\d{10}$/

const ROSTER_ROLES = ['headcoach', 'president', 'player', 'retired']

const USER_ROLES = ['basic', 'member', 'manager', 'staff', 'root']

const STAFF_TYPES = ['president', 'headcoach']

const MANAGER_ROLES = ['manager', 'staff', 'root']

module.exports = { STUDENT_ID_REGEX, ROSTER_ROLES, USER_ROLES, STAFF_TYPES, MANAGER_ROLES }
